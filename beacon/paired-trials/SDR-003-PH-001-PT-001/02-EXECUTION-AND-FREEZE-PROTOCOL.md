# PTR-001 — Execution and Freeze Protocol

> **PTR-001 v0.1 — GOOGLE GEMINI OPAQUE PAIRED TRIAL PREPARED — EXECUTION NOT AUTHORIZED**

## Frozen nonrevealing chronology

The private package freezes one cryptographically random opaque A/B execution order. The order and baseline/treatment mapping are not public.

For each condition, a later authorization must require:

1. A fresh browser-incognito session and fresh Gemini Temporary Chat.
2. The same displayed model 3.1 Pro with Extended Thinking enabled.
3. No prior chat, other condition, personalization, custom context, connected app, manually activated search, or other tool.
4. One condition-specific file upload and one exact substantive message.
5. No greeting, follow-up, clarification, correction, or evaluation request.
6. Complete observable output and evidence freeze before validity classification.
7. Validity, exposure, and contamination classification before any technical-retry decision.
8. At most one technical retry for that condition and zero substantive retries.
9. One isolated condition-specific scoring context, with its categorical Levels 0–4 score frozen before comparison.
10. Opaque A/B comparison only after both scores freeze, followed by any separately authorized private unblinding.

The complete copyable final response is the primary scorable output. Automatically visible reasoning summaries, video, screenshots, interface events, and provider incident messages are auxiliary evidence. Visible search, retrieval, grounding, citations, connected context, or tool use is contamination and is not a reason to rerun for a cleaner result.

The first condition may have no known prior provider exposure. The second condition's first submission must record prior-other-condition provider exposure. A technical retry must record prior-same-condition provider exposure. Temporary Chat isolation and backend identity remain unproven.

Level 1 is the primary endpoint; Levels 0 and 2 are nonregression guardrails; Levels 3 and 4 are exploratory. There is no aggregate score. Both candidate-specific scores must freeze before comparison and unblinding.

## Stop boundary

- Valid outputs requested: 2
- Technical retries maximum: 1 per condition
- Substantive retries: 0
- Authorized incremental spend: USD 0
- Current executions, outputs, validity classifications, scores, comparisons, unblindings, and SIG-002 attempts: 0

If the exact configuration is unavailable or requires payment, execution stops and the package may remain dormant. D-032 does not authorize performing this protocol.

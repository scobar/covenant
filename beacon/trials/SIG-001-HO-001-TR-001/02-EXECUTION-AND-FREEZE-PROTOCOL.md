# SIG-001-HO-001-TR-001 — Execution and Freeze Protocol

> **PREPARED HOLDOUT DECODING TRIAL SIG-001-HO-001-TR-001 v0.1 — NONCANONICAL — NOT EXECUTED**

This public record states only nonrevealing procedural rules. The exact inputs,
private identities, local paths, scorecard answers, mapping, and solution
remain private.

## Preconditions

- The complete public D-023 preparation package is reviewed and committed
  before execution.
- The primary sealed package and private trial package remain intact outside
  Git.
- Private custody-integrity verification confirms that the sealed files,
  manifests, preimages, and commitments still match without revealing private
  material to the decoder or scoring context.
- The verified removable-drive backup remains disconnected.
- The qualification-only chat is closed without receiving input.
- A new, empty Claude Incognito Chat is created outside every Project.
- The displayed model label is `Fable 5` in ordinary claude.ai standard Chat.
- Default response style is selected.
- Web search and all connectors are disabled.
- Project context, Claude Code, Cowork, Claude Desktop, browser control,
  repository access, local-filesystem access, and unrelated tools are absent.
- Promotional Fable 5 credit remains available.
- Auto-reload remains off and paid spillover remains disabled.
- Any payment, upgrade, API, metered-use, or credit-purchase prompt stops the
  trial.

Exact browser, operating-system, visible UI, tool, time, and credit metadata
are captured privately immediately before execution. If any frozen
configuration or USD `0` gate cannot be met, the trial stops. No provider,
model, interface, tool, API, or paid route may be substituted.

## Input and interaction

Only the frozen sealed neutral instruction and sequence are supplied. The
sequence is the sole uploaded file. The complete instruction is the entire
single user message. The operator adds no greeting, explanation, project
name, file description, identifier, commitment, target level, target result,
hint, scorecard, mapping, or solution.

The message is sent exactly once. No second message, correction, answer to a
decoder question, tool activation, or follow-up is permitted.

## One run and technical retry

One valid run is requested and every run is preserved. Refusal, failure,
partial decoding, criticism, indeterminacy, and unexpected interpretation are
valid substantive outputs. None permits regeneration or a retry.

At most one retry is allowed solely for a frozen technical-invalidity
condition, such as transport failure, failed input delivery, wrong input,
unrecoverable pre-response truncation, inability to preserve the response,
prohibited context supplied before substantive processing, or a displayed
model change before substantive processing. The invalid run and incident are
preserved. A retry uses a new Incognito Chat, identical inputs and
configuration, Fable 5, the USD `0` route, and no clues from the invalid run.
If it also fails, the trial remains incomplete.

## Output and reveal chronology

Private custody-integrity verification is a private, nonrevealing
recomputation by the authorized custodian confirming that sealed files,
manifests, preimages, and commitments still match. It is required during
preparation and immediately before execution. It reveals nothing publicly,
supplies no mapping, solution, scorecard, preimage, private identity, or
holdout content to the decoder or pre-output-freeze scoring context, is not
reveal-stage verification, and creates no decoding result.

Reveal-stage commitment verification is the formal comparison performed only
after both the complete raw output and complete score have frozen. Under later
authorization, the relevant private preimages, manifests, mapping, solution,
and scorecard materials are revealed and compared with their public
commitments. Every mismatch or failed verification is preserved. Public
recording requires separate authorization, and verification cannot
retroactively change the frozen output or score.

The required chronology is:

1. Freeze private configuration and packet.
2. Perform private custody-integrity verification.
3. Publish and commit the public preparation package.
4. Reconfirm private custody-integrity verification immediately before
   execution.
5. Execute exactly one permitted decoder run.
6. Freeze the complete raw output and execution metadata.
7. Perform validity classification and scoring.
8. Freeze the complete score.
9. Reveal the authorized private preimages, manifests, mapping, solution, and
   scorecard materials.
10. Perform reveal-stage commitment verification.
11. Preserve every mismatch, deviation, failure, and alternative
    interpretation.
12. Consider publication only through a later explicit decision.

The operator waits for the response to complete, sends no follow-up, and uses
the browser's response-copy control. The observable browser-copy
representation and complete execution metadata are preserved privately
without silent editing. Private screenshots are secondary evidence. No score
or reveal occurs in the decoder conversation. The output is not supplied to
ChatGPT before score freeze. No private solution or mapping may be supplied to
Claude. There is no selective rerun or publication.

## Current status and inference boundary

Preparation is complete only after all package audits pass. The decoder
configuration is frozen. No Claude execution chat was opened or used, no
input was supplied, and no output, score, reveal, or empirical result exists.

Browser-copy preservation may omit hidden formatting or metadata. The
displayed model label is not an independently verified backend model ID,
Incognito is not proof of absent hidden context, and promotional credit does
not create independent diversity. One run is one bounded datapoint. No
successful lower-level result establishes Levels 5–9; failure does not
establish universal undecodability. No candidate, Covenant, CSR, physical,
agency, choice, response, carrier, distribution, or transmission conclusion
follows.

# SDR-003 — Public Serialized-Pair Charter

> **SDR-003 v0.1 — SURFACE-NOVEL OPAQUE PAIRED HOLDOUTS PRIVATELY FROZEN — NO PROVIDER, TRIAL, OUTPUT, SCORE, UNBLINDING, OR SIG-002 ATTEMPT**

## Identity and lifecycle

- Package: `SDR-003 v0.1`.
- Pair ID: `SDR-003-PH-001`.
- Parent exact design: `SDR-002 v0.1`.
- Selected family: `Nested local framing signatures`.
- Lifecycle:
  `Surface-serialized opaque pair privately frozen — no provider or trial`.
- Opaque conditions: `Condition A` and `Condition B`.
- Condition mapping: private.
- Authorized incremental spend: USD `0`.

## Purpose

SDR-003 realizes the privately frozen SDR-002 symbolic pair as two exact,
surface-novel condition streams under one shared profile. Both streams froze
before a fresh random assignment to their opaque labels. They are sealed,
unassigned successor holdouts, not public signal attempts or trial packets.

## Controlled boundary

The surface pair preserves all eighteen SDR-002 invariants. The same semantic
graph, truth assignments, quantity domain, curriculum, examples, queries,
token roles and arities, duplicate-body policy, instruction, scorecard logic,
and validity logic apply to both conditions.

Every framing-cue instance has a one-to-one counterpart. Cue values and
multiplicities, the total framing-event count and byte budget, and all stable
nonframing content match exactly. Total length and event counts match, and
each condition has one intended parse. The only designed difference is the
frozen placement of existing framing cues: global concentration versus nested
locality. This is formal design control, not causal or empirical proof.

Level 1 framing and hierarchy recovery remains the primary endpoint. Level 0
artificiality and Level 2 structured numeracy remain nonregression guardrails.
Levels 3 and 4 remain exploratory. Levels 5–9 are absent.

## Secrecy and nonexecution

Surface representations, serialized bytes and identities, exact lengths,
profile and generator details, solutions, scorecard details, cue and slot
ledgers, private mapping, private manifest identity, preimages, and nonces
remain outside Git. Public commitments detect later substitution but do not
reveal those values.

Providers, models, trial packets, trials, outputs, validity classifications,
scores, comparisons, unblindings, and SIG-002 attempts are all `0`. No higher
layer, Covenant or CSR payload, physical or agency profile, response syntax,
carrier, distribution, or transmission was created or authorized.
